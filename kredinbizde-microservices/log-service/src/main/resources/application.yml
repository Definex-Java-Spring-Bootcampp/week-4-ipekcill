spring:
  application:
    name: log-service
  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27017/logdb
  kafka:
    consumer:
      auto-offset-reset: latest
      group-id: consumer-group
      bootstrap-servers: localhost:9092
      topic:
        name: log-topic

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka